<?xml version='1.0' encoding='UTF-8' ?>
{% load i18n %}
<xforms xmlns="http://openrosa.org/xforms/xformsList">
 {% for household in households %}
  <xform>
    <formID>{{household.get_head.surname}} ({{ household.uid }}) </formID>
    <name>{{ household.survey.name }}</name>
    <majorMinorVersion/>
    <version/>
    <hash />
    <descriptionText> {{ household.survey.description }}</descriptionText>
    <downloadUrl>{{ base_url }}{% url 'download_odk_survey_form' household.uid %}</downloadUrl>
    <manifestUrl></manifestUrl>
  </xform>
{% endfor %}
</xforms>
