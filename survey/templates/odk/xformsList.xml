<?xml version='1.0' encoding='UTF-8' ?>
{% load i18n %}
<xforms xmlns="http://openrosa.org/xforms/xformsList">
 {% for survey in surveys %}
  <xform>
    <formID>{{ survey.pk }} </formID>
    <name>{{ survey.name }}</name>
    <majorMinorVersion/>
    <version/>
    <hash />
    <descriptionText> {{ survey.description }}</descriptionText>
    <downloadUrl>{{ base_url }}{% url 'download_odk_survey_form' survey.pk household_size %}</downloadUrl>
    <manifestUrl></manifestUrl>
  </xform>
{% endfor %}
</xforms>
